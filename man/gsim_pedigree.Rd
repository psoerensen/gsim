% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pedigree.R
\name{gsim_pedigree}
\alias{gsim_pedigree}
\title{Simulate a deterministic multigenerational pedigree workload}
\usage{
gsim_pedigree(
  n_animals = NULL,
  n_generations = 5L,
  animals_per_generation = 200L,
  founder_generations = 2L,
  sires_per_generation = NULL,
  dams_per_generation = NULL,
  overlapping_generation_probability = 0.2,
  unknown_sire_probability = 0.02,
  unknown_dam_probability = 0.02,
  new_founder_probability = 0.01,
  unphenotyped_probability = 0.15,
  permute_external_order = TRUE,
  seed = NULL
)
}
\arguments{
\item{n_animals}{Optional total number of animals. When supplied, animals are
distributed as evenly as possible over \code{n_generations}.}

\item{n_generations}{Number of generations.}

\item{animals_per_generation}{Generation size when \code{n_animals} is \code{NULL}.}

\item{founder_generations}{Number of initial all-founder generations.}

\item{sires_per_generation, dams_per_generation}{Maximum sizes of the
sex-specific parent pools from each eligible generation. \code{NULL} selects a
small scaling default.}

\item{overlapping_generation_probability}{Probability of choosing an
available parent from two generations back rather than one.}

\item{unknown_sire_probability, unknown_dam_probability}{Parent-specific
missingness probabilities.}

\item{new_founder_probability}{Probability that a later animal has neither
recorded parent.}

\item{unphenotyped_probability}{Probability that an animal is marked as
unavailable for observed-record generation.}

\item{permute_external_order}{Reproducibly permute the returned pedigree rows.}

\item{seed}{Optional nonnegative integer seed.}
}
\value{
An object of class \code{gsim_pedigree} containing the externally ordered
pedigree, canonical and external ID vectors, an explicit order mapping,
settings, diagnostics, and deterministic checksums.
}
\description{
\code{gsim_pedigree()} creates a linear-storage pedigree domain for pedigree-model
and sparse-solver validation. It never constructs a relationship matrix. The
returned pedigree table is in reproducibly arbitrary external order;
\code{canonical_order} is topological and \code{mapping} links both orders.
}
\details{
A supplied \code{seed} calls \code{\link[=set.seed]{set.seed()}} and the call then advances the caller's
RNG state. Without a seed, the current R RNG stream is used. \code{RNGkind()} and
the supplied seed are recorded.
}
\examples{
ped <- gsim_pedigree(n_generations = 5, animals_per_generation = 40,
                     sires_per_generation = 6, dams_per_generation = 12,
                     seed = 101)
head(ped$pedigree)
}
