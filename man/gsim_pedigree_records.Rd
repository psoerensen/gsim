% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pedigree.R
\name{gsim_pedigree_records}
\alias{gsim_pedigree_records}
\title{Simulate observation records from a gsim pedigree}
\usage{
gsim_pedigree_records(
  pedigree,
  model = c("single_trait", "multitrait", "longitudinal"),
  fixed_effects = NULL,
  genetic_covariance = NULL,
  residual_covariance = NULL,
  residual_variances = NULL,
  random_regression_rank = 3L,
  basis_function = NULL,
  minimum_records = 1L,
  maximum_records = 5L,
  residual_groups = 3L,
  prediction_records = FALSE,
  permute_record_order = TRUE,
  seed = NULL
)
}
\arguments{
\item{pedigree}{A \code{gsim_pedigree} object.}

\item{model}{One of \code{"single_trait"}, \code{"multitrait"}, or \code{"longitudinal"}.}

\item{fixed_effects}{Optional finite vector with one value per generated
design column, unnamed in design order or named by all design columns.}

\item{genetic_covariance}{Genetic covariance (single/multitrait) or random-
regression coefficient covariance (longitudinal).}

\item{residual_covariance}{Residual covariance for single/multitrait models.}

\item{residual_variances}{Longitudinal residual-group variances.}

\item{random_regression_rank}{Basis rank from one to three.}

\item{basis_function}{Optional longitudinal function accepting the time vector
and returning a finite matrix with \code{random_regression_rank} columns. \code{NULL}
uses the documented shifted-Legendre basis.}

\item{minimum_records, maximum_records}{Longitudinal observed records per
phenotyped animal.}

\item{residual_groups}{Positive number of residual groups.}

\item{prediction_records}{Whether to return longitudinal prediction records.}

\item{permute_record_order}{Reproducibly permute scalar record order.}

\item{seed}{Optional nonnegative integer seed.}
}
\value{
An object of class \code{gsim_pedigree_records} with the original pedigree,
scalar records, sparse fixed-design triplets, optional basis and prediction
records, model truth, settings, diagnostics, and deterministic checksums.
}
\description{
\code{gsim_pedigree_records()} creates one scalar-long model view from a supplied
\code{\link[=gsim_pedigree]{gsim_pedigree()}} domain. Missing traits and times are represented by absent
rows, never imputed phenotypes. The fixed design is returned as sorted,
one-based sparse triplets rather than a dense matrix.
}
\details{
Latent animal values are deterministic recursive solver-workload values.
They are deliberately not claimed to be exact draws from a numerator
relationship covariance: \code{truth$exact_relationship_draw} is \code{FALSE}, and
covariance recovery is not a validation target. They are appropriate for
comparing solvers supplied with identical equations and right-hand sides.

A supplied \code{seed} calls \code{\link[=set.seed]{set.seed()}} and the call then advances the caller's
RNG state. Optional longitudinal prediction records contain animal, time,
basis and fitted truth, but no residual or phenotype.
}
\examples{
ped <- gsim_pedigree(n_generations = 4, animals_per_generation = 20, seed = 1)
dat <- gsim_pedigree_records(ped, model = "longitudinal",
                             prediction_records = TRUE, seed = 2)
head(dat$records)
}
