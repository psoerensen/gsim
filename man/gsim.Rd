% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsim.R
\name{gsim}
\alias{gsim}
\title{Simulate genomic data from an in-memory matrix or a qgg Glist}
\usage{
gsim(
  Glist = NULL,
  W = NULL,
  ids = NULL,
  rsids = NULL,
  A = NULL,
  architecture = c("bayesc", "bayesr", "major_polygenic", "maf_dependent", "clustered",
    "fixed"),
  nt = 1L,
  h2 = 0.5,
  vg = 1,
  pi = NULL,
  mixture_variances = NULL,
  causal_probability = NULL,
  marker_multipliers = NULL,
  n_causal = NULL,
  beta = NULL,
  alpha = NULL,
  annotation_model = c("none", "sbayesrc"),
  rg = NULL,
  re = 0,
  maf = NULL,
  a = 0,
  b = 0,
  c = 0,
  ld_score = NULL,
  annotation_score = NULL,
  maf_exponent = -0.5,
  block_id = NULL,
  n_hot_blocks = NULL,
  cluster_enrichment = 20,
  n = NULL,
  m = 1000L,
  maf_min = 0.05,
  maf_max = 0.5,
  n_annotations = 0L,
  annotation_types = NULL,
  annotation_prob = 0.1,
  seed = NULL,
  standardize_W = TRUE,
  scale_effects = TRUE,
  return_genotypes = FALSE,
  return_marker_probabilities = TRUE,
  compute_sumstats = FALSE,
  chunk_size = 5000L,
  getG_fun = NULL
)
}
\arguments{
\item{Glist}{Optional qgg genotype-list object.}

\item{W}{Optional in-memory genotype matrix. At most one of \code{Glist} and \code{W}
may be supplied. If both are NULL, independent binomial genotypes are
simulated.}

\item{ids}{Optional sample IDs. In Glist mode, \code{n} randomly selected IDs are
used when \code{ids} is NULL and \code{n} is smaller than the available sample size.}

\item{rsids}{Optional eligible marker IDs.}

\item{A}{Optional marker-by-annotation matrix. Row names, when present, are
matched to marker IDs. Binary and continuous columns may be mixed.}

\item{architecture}{One of \code{"bayesc"}, \code{"bayesr"},
\code{"major_polygenic"}, \code{"maf_dependent"}, \code{"clustered"}, or \code{"fixed"}.}

\item{nt}{Number of traits.}

\item{h2}{Target trait heritabilities.}

\item{vg}{Target genetic variances. Marker effects are rescaled to these
realized variances when \code{scale_effects = TRUE}.}

\item{pi}{Mixture probabilities, with the null component first.}

\item{mixture_variances}{Relative mixture-component variances, with zero
first.}

\item{causal_probability}{Optional scalar or marker-specific probability
\eqn{q_j} that marker \eqn{j} is non-null. A marker-specific vector must
have unique nonempty names exactly matching the simulation markers. When
supplied, these probabilities replace the null mass implied by \code{pi}, while
\code{pi[-1] / sum(pi[-1])} retains the conditional probabilities of the active
mixture components. This is a Bernoulli model and cannot be combined with
\code{n_causal}, \code{architecture = "fixed"}, \code{architecture = "clustered"}, or
annotation-driven component probabilities.}

\item{marker_multipliers}{Optional positive marker-specific relative
active-effect variance weights, \eqn{w_j}. A scalar is repeated; a
marker-specific vector must have unique nonempty names exactly matching
the simulation markers and is aligned once to canonical marker order.
Direct multipliers cannot be combined with nonzero \code{a}, \code{b}, or \code{c}.
The multiplier is applied only to non-null effect draws, separately from
component probabilities and the component multiplier. \code{NULL} gives unit
multipliers when the derived model is not requested. Values are not
clipped or normalized. Non-unit multipliers are not defined for
\code{architecture = "fixed"}, where \code{beta} supplies realized effects directly.}

\item{n_causal}{Optional exact number of non-null markers. If supplied,
markers are sampled without replacement using their annotation-informed
non-null probabilities.}

\item{beta}{Fixed marker effects for \code{architecture = "fixed"}.}

\item{alpha}{Annotation effects, with dimensions \code{ncol(A)} by
\code{length(pi)-1}. Effects act on centered annotations and continuation logits.}

\item{annotation_model}{\code{"none"} or \code{"sbayesrc"}.}

\item{rg, re}{Genetic-effect and residual correlation matrices (or scalar
off-diagonal correlations for multiple traits).}

\item{maf}{Optional marker MAF. A scalar is repeated; a marker-specific
vector must be named by marker ID. In Glist mode, \code{Glist$maf}, aligned
through \code{Glist$rsids}, supplies MAF when \code{a} is nonzero and \code{maf} is NULL.}

\item{a, b, c}{Finite scalar exponents in the marker-specific conditional
effect-variance model
\eqn{w_j=[p_j(1-p_j)]^a r_j^b s_j^c}. All default to zero. The model is
requested when any exponent is nonzero.}

\item{ld_score}{Optional marker LD score \eqn{r_j}. A scalar is repeated; a
marker-specific vector must be named by marker ID. In Glist mode,
\code{Glist$ldscores}, aligned through \code{Glist$rsidsLD}, is used when \code{b} is
nonzero and \code{ld_score} is NULL. LD scores are not calculated or floored.}

\item{annotation_score}{Optional single positive scalar annotation score
\eqn{s_j} per marker. A scalar is repeated; a marker-specific vector must
be named by marker ID. This controls conditional effect variance only and
is never inferred from the SBayesRC \code{A} matrix.}

\item{maf_exponent}{For \code{maf_dependent}, effect variance is proportional to
\verb{[2p(1-p)]^maf_exponent} before realized-variance calibration.}

\item{block_id}{Optional marker block labels for \code{clustered}.}

\item{n_hot_blocks}{Number of enriched blocks in the clustered architecture.}

\item{cluster_enrichment}{Active-odds multiplier in enriched blocks.}

\item{n, m}{Sample and marker counts when genotypes are simulated. In Glist
mode, \code{n = NULL} uses all available samples.}

\item{maf_min, maf_max}{MAF range for simulated genotypes.}

\item{n_annotations}{Number of annotations to simulate when \code{A} is NULL.}

\item{annotation_types}{Simulated annotation types: \code{"binary"} and/or
\code{"continuous"}.}

\item{annotation_prob}{Probability for simulated binary annotations.}

\item{seed}{Optional seed.}

\item{standardize_W}{Whether loaded causal genotypes are standardized.}

\item{scale_effects}{Whether effects are calibrated to \code{vg}.}

\item{return_genotypes}{Return causal genotype columns in \code{W_causal}.}

\item{return_marker_probabilities}{Return the full marker probability surface.}

\item{compute_sumstats}{Compute marginal GWAS statistics. Glist data are read
in chunks for this optional step.}

\item{chunk_size}{Maximum marker columns requested in each Glist read.}

\item{getG_fun}{Optional replacement for \code{qgg::getG}, primarily for tests.}
}
\value{
A list containing phenotypes, genetic values, residuals, exact marker
effects and states, annotation truth, probability surfaces, the complete
canonical \code{causal_probability} and \code{marker_multipliers} vectors, compact
provenance under \code{settings$causal_probability} and
\code{settings$marker_multipliers} (including the variance formula, exponents,
metadata sources, and alignment), and optional summary statistics.
}
\description{
\code{gsim()} simulates genomic data for validation and methodological studies. In
Glist mode, causal variants are selected from marker metadata before any
genotypes are read; only those causal columns are requested from
\code{qgg::getG()} to construct genetic values. Genome-wide genotypes are read in
chunks only when \code{compute_sumstats = TRUE}.
}
