% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcf_public.R
\name{gsim_simulate_founders}
\alias{gsim_simulate_founders}
\title{Simulate a reusable phased synthetic base population}
\usage{
gsim_simulate_founders(
  reference,
  n = NULL,
  founder_ids = NULL,
  populations,
  ancestry_weights,
  mutation_age,
  N,
  Ne,
  rho,
  seed,
  output,
  batch_size = NULL,
  threads = 1L,
  overwrite = FALSE
)
}
\arguments{
\item{reference}{A \code{\link[=gsim_reference]{gsim_reference()}} descriptor or HAP/BIM/FAM prefix.}

\item{n}{Number of founders. Exactly one of \code{n} and \code{founder_ids} is used.
Generated IDs are \code{syn1}, \code{syn2}, and so on.}

\item{founder_ids}{Explicit unique character founder IDs in final sample
order.}

\item{populations}{Population labels named by reference FAM IID.}

\item{ancestry_weights}{Named donor-population probabilities.}

\item{mutation_age}{Finite mutation ages named by reference BIM ID.}

\item{N, Ne, rho}{Named HAPNEST population parameters in the committed
cumulative-cM founder convention.}

\item{seed}{Founder-generation seed.}

\item{output}{Extension-free HAP/BIM/FAM output prefix.}

\item{batch_size}{Positive requested founder batch size. Internal batches are
aligned to packed 64-sample words; \code{NULL} uses a bounded default.}

\item{threads}{Positive native founder worker count.}

\item{overwrite}{Whether to replace an existing complete triplet.}
}
\value{
A lightweight reusable phased base-population descriptor.
}
\description{
Generates unrelated founders from a packed reference panel using the
HAPNEST-compatible historical copying model. Output is always phased
HAP/BIM/FAM so that the base population can be reused by independent
pedigree simulations.
}
\details{
Founder H1 copies only donor H1 and founder H2 only donor H2 under
the chromosome-label-keyed SplitMix64 contract and strict
\code{T < mutation_age} filtering. \code{batch_size = NULL} requests 8,192 founders;
explicit sizes are rounded up to a 64-sample word boundary and the last batch
may be partial. Each batch is materialized by static native workers that own
disjoint packed words, then written directly to its final HAP sample range.
Batch size and thread count do not enter RNG streams. The R global RNG is not
used.
}
